import {
  saveScrollActions,
  saveScrollReducer,
} from './model/slices/SaveScrollSlice';
import { SaveScrollSchema } from './model/types/SaveScrollSchema';
export { SaveScrollSchema, saveScrollReducer, saveScrollActions };
